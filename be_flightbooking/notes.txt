(Chạy BE)
# Development - Cơ bản (hiện tại)
uvicorn main:app --reload

# Development - Tối ưu (khuyên dùng) - FIXED
uvicorn main:app --reload --host 127.0.0.1 --port 8000 --reload-dir routers --reload-dir models --reload-dir utils --log-level info

# Development - Debug mode
uvicorn main:app --reload --log-level debug --use-colors --access-log

# Production - Single worker
uvicorn main:app --host 0.0.0.0 --port 8000 --workers 1

# Production - Multiple workers (khuyên dùng cho production)
gunicorn main:app -w 4 -k uvicorn.workers.UvicornWorker --bind 0.0.0.0:8000

Kích hoạt môi trường 
.\.venv\Scripts\activate

# Cài thêm gunicorn cho production
pip install gunicorn
